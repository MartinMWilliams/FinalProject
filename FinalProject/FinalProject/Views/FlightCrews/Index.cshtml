@model IEnumerable<FinalProject.Models.FlightCrew>

@{
    ViewBag.Title = "Index";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.Pilot)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.CoPilot)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.CabinCrew)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.Pilot)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.CoPilot)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.CabinCrew)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.FlightCrewID }) |
            @Html.ActionLink("Details", "Details", new { id=item.FlightCrewID }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.FlightCrewID })
        </td>
    </tr>
}

</table>
